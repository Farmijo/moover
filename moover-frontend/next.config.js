/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuración básica para desarrollo con Docker
  // No necesitamos configuración especial de Turbopack para el live reloading
}

module.exports = nextConfig
